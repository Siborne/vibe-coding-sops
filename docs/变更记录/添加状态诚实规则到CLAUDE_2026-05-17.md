# 添加 Status Honesty 规则到 CLAUDE.md Rules to Load 表

## What
- **File**: `CLAUDE.md`
- **Change**: 在 `## Rules to Load` 表格末尾新增一行，添加规则 #7 Status Honesty 的条目。

## Why
- 仓库新增了 `rules/status-honesty.md` 规则文件，按 CLAUDE.md 自身的 Adding a New Rule 流程，第 4 步需要更新 Rules to Load 表。
- 该规则要求每次代码变更后上报四种状态之一（DONE / PENDING VERIFICATION / BLOCKED / PARTIAL），DONE 必须附带验证清单。

## Before/After
不适用（纯新增行，无结构性改写）。
